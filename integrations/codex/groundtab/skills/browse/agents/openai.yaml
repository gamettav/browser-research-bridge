# GENERATED from skills-src/browse.mjs by scripts/generate-skills.mjs. Do not edit; edit the canonical file and run `pnpm generate:skills`.
interface:
  display_name: "Browse"
  short_description: "Research the web and answer with cited sources, through the local browser bridge."
  default_prompt: "Research this topic through GroundTab and answer with cited final source URLs."
dependencies:
  tools:
    - type: mcp
      value: groundtab
      description: "Read-only GroundTab tools: web search and rendered-page fetch."
policy:
  allow_implicit_invocation: true
