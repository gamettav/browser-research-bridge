{
  "name": "@browser-research/chrome-extension",
  "version": "0.4.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@browser-research/protocol": "workspace:*",
    "@mozilla/readability": "^0.6.0",
    "turndown": "^7.2.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.1",
    "@types/turndown": "^5.0.5",
    "esbuild": "^0.25.8",
    "vitest": "^3.2.4"
  }
}
